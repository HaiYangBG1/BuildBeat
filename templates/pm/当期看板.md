# <期名> · 协调看板

> 谁做什么、什么顺序、卡点在哪。工作包完成口径 = **用户级结果 + commit hash + 可核验证据**(核查门);子任务完成不等于会话结束。
> 线上实况:`bash scripts/bus-check.sh`。拍板记 `decisions.md`。

## 当前工作包(每个会话同时只认领一个;不同目标可有少量并行包)

### WP-1 · <工作包名>

- **objective**:<这轮最终要交付什么结果,不用文件名代替>
- **参与域**:<产品 / 全栈 / 测试;各自仍只写自己的边界>
- **in_scope**:<为达成结果可自动继续的关联任务/域/文件边界>
- **terminal_condition**:<带证据完成 / 必须由人处理的真实阻塞 / 用户明确的检查点>
- **状态**:🔄进行 / ⏸阻塞 / ✅完成

> 同一工作包可由多个域接力;复制本小节即可增加并行包,但不要按文件造包。原子 commit 可以细;单个文档提交、reviewer 返回、status 回写、普通 P2 只是包内事件。只要还有安全、可逆、在 `in_scope` 内的工作,会话继续推进,不结束等用户说“继续”。

## 决策收件箱(只收 BATCH_AT_GATE;默认一次提交 2–5 个真实取舍,确实只有 1 个就单项)

| 包ID | 变量ID | 真实取舍 | 推荐值 + 理由 | 另一选择的后果 | 截止 Gate | 状态 |
|---|---|---|---|---|---|---|
| DP-<Gate>-01 | D1 | | | | Gate1 / Gate2 / Gate3 | 待批 / 已收敛 |

> 能实查的事实、已批准范围内的推导结论和普通实现细节不进收件箱;STOP_NOW 事项立即停在动作前问,不可混在批量队列里。用户分轮回答/要求解释时沿用同一包ID、只更新本表;整个决策包收敛后再在 `decisions.md` 记一行。

## 阶段门
| 阶段 | 产出 | 负责 | 状态 |
|---|---|---|---|
| ① 需求(⛔Gate1 人批) | 需求与决策 | 产品 | ☐ |
| ② 设计(⛔Gate2 人对**真渲染原型**批,规则⑩) | `design/design_<N>期/`(含可渲染 HTML) | 外部设计工具 | ☐ |
| ③ 实现 | 代码 + 契约 + 实现语义清单;机器闸每提交 | 全栈 | ☐ |
| ④ 验证 + 核查门 | review-ready(`HEAD=candidate` + 干净 + L3/渲染绿 + 无待修) + milestone **一次** + P0/P1 合并 closure | 测试 + 产品 | ☐ |
| ⑤ 上线(⛔Gate3 合并 / ⛔Gate4 上线,人批) | 部署 + CHANGELOG | 全栈 | ☐ |

## 分工总表(需求 → 谁做)
| 需求 | 全栈 | 测试 | 备注 |
|---|---|---|---|
| <需求1> | ★ | E2E+走查 | |

## 🔴 关键对齐点(开工先定,别各做各的)
1. <跨边界接口/字段/机制 约定;契约口径落 `contracts/PROTOCOL.md`>

## 挂账(实时;各域认领后状态写回自己的 status,此处只改 ☐/✅)
| # | 项 | 域 | 状态 | 依据/出处 |
|---|---|---|---|---|
| 1 | | | ☐ | |

## 验收口径
<对照需求清单 + 设计稿逐屏;上线前必过的清单>

> 核查节奏:首次 milestone 前写者先收敛,文件数/草稿/归档/status/P2 不触发 reviewer;每工作包每 Gate 默认一次 milestone,P0/P1 合并修完后一次 closure。同一 candidate 合并前复用结论;reviewer 返回前 hash 变化则旧审查 `SUPERSEDED`,不得边改边 delta。
