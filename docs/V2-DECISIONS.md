# BuildBeat v2 决策台账

> 只记录已收敛、会改变 v2 方向或授权边界的真实拍板；实现细节和试点数据留在对应迭代与证据文件中。

| 日期 | 决策 ID | 拍板人 | 决策 | 生效结果 | 后续决策点 |
|---|---|---|---|---|---|
| 2026-08-27 | `V2-D1A` | 项目所有者 | M-1 第三个目标项目选定为 `AI底座/底座`；该项目开发活跃，已按 legacy 路径手工接入 BuildBeat 1.x，适合验证旧项目、多仓与接力棒场景 | 既有 `WP-B1-AUTHZ` 只作回放/兼容性证据，不倒算为自动 Loop；第三个有效 run 使用该项目下一项自然发生、已授权、低风险的非生产开发任务，并从干净 oracle commit 启动。当前不原地升级目标项目、不补造 manifest、不改/关现有 Gate，也不授权 push、merge、migration、部署或生产动作 | 有合适工作项时冻结 intent/plan/oracle；若跨多仓，先让试点明确绑定全部 workspace，不能静默退化成单仓证据 |
| 2026-08-27 | `V2-D1` | 项目所有者 | M-1 首批选择两个真实工作项：① ChickAI 额度耗尽时仍可正常登录网站，但模型不可用且不得补发 Key 绕额度；② Tide 悬浮球空闲时缩入左侧并半透明隐藏，交互时立即展开 | 迭代 01 从本日启动；两项均只允许在本地 `pilot/*` 分支实施与验证，不授权 merge、push、发布、部署或生产操作；第三个工作项暂留空，不影响先跑前两轮 | 两轮证据完成后决定是否补第三轮；最终形成 `V2-D2` |
| 2026-08-27 | `V2-D0` | 项目所有者 | 选择方案 **B**：正式采用 [`V2-PLAN.md`](V2-PLAN.md) 作为 BuildBeat v2 执行基线，不再把 M-1 当作“是否做 v2”的试验 | v1 转入维护线；建立本地 `v1-maintenance` 与 `v2` 开发线；`V2-PLAN.md` D1–D6 推荐项同步生效；M-1 只决定薄内核或完整内核；本决策不授权 push、发布、部署或生产操作 | `V2-D1`：M-1 工作项；`V2-D2`：M-1 分叉结论 |
