# v3.0.1 发布证据（2026-09-09）

> 授权：所有者会话内「合并 #39，改掉那行过期注释，然后更新指南，然后发 3.0.1」；稳定版通道：从 `main` 顶端以稳定 tag 发布到 dist-tag `latest`。

- 内容：PR #39（示例项目 `example/`：虚构单仓「简账」跑完一个 Work 到合并决定的快照，含运行时真跑产出的 `decisions.jsonl` 与 `run-record.json`，`tests/example-firstrun.test.js` 锁住）、PR #40（快速开始 / Human Approval / Evidence / 故障恢复四篇英文版；快速开始安装注释改为 3.0.0 实际打印的 `BuildBeat runtime`）。运行时零改动。
- 路径：release 提交 `89ffdae` 在 `release/3.0.1`（版本号、CHANGELOG 标题、`docs/RELEASING.md` 源版本行）经 PR #41 以 merge commit `c322ce9` 合入 `main`，annotated tag `v3.0.1` 指向它。本地候选门全过：`npm ci`、`bash -n`、`shellcheck -x`、`actionlint`、`check-docs`（123 文件）、`npm test`（含示例首跑 2 条）、`test:pilot`、`test:plugin`、gitleaks 无泄漏、`git diff --check`、`npm publish --dry-run`（含 `prepublishOnly` 全链，`test:pack-firstrun` 17 条）；PR #39/#40/#41 与 `main` 顶端 CI/CodeQL 全部 success。发布前回读（直连 registry.npmjs.org）：`3.0.1` 不存在（E404）；dist-tags `{bootstrap: 0.0.0, next: 3.0.0, latest: 3.0.0}`；tag ruleset `Protect release tags`：active、`refs/tags/v*`、`update,deletion`、bypass 为空。
- 发布：workflow run [34370800960](https://github.com/HaiYangBG1/BuildBeat/actions/runs/34370800960)（`workflow_dispatch` from `main`，tag `v3.0.1`，稳定通道 → `latest`）双 job 一次 success（`Publish v3.0.1 to npm` + `Verify v3.0.1 from npm`；3.0.0 closeout 把回读窗口拉到 10 分钟后，本次 verify 未再超时）；`npm-publish` 环境审批按会话授权以所有者 gh 凭据落章，备注引用授权原文与候选 integrity。
- 本地独立回读（直连 registry.npmjs.org，不经镜像）：dist-tags `{bootstrap: 0.0.0, next: 3.0.0, latest: 3.0.1}`；`dist.integrity = sha512-+gKb0hc2c5+/OUW+LhMC48tMT1WFuxJCpYMrHiTXtdCW9AVPJ3HQLht5Apk7v1vU5uwbs8+NVxgZ4JfV2PDQCw==`（与发布前本地 `npm pack --dry-run --json` 逐字相同，shasum `b0008cfd`），`fileCount 112`、`unpackedSize 657181`；`dist.attestations.url` 存在；隔离前缀安装 `@3.0.1` → `bin/` 下只有 `buildbeat`，`buildbeat --version = 3.0.1`，裸调用 exit 0、对空目录零写入；包内 `example/` 23 个文件（含 run-record）与四篇 `.en.md` 指南在位；临时项目 `npm audit signatures`：1 package has a verified registry signature / verified attestation。
- GitHub：Release [v3.0.1](https://github.com/HaiYangBG1/BuildBeat/releases/tag/v3.0.1) 已建并标 **Latest**（`releases/latest` 回读 = v3.0.1；此前 Latest 为 v3.0.0）。
- 切换：所有者本机全局 CLI `npm install --global @haiyangbg/buildbeat@latest` → 3.0.1；`v2` 分支 ff 到 `main`。
- 未做：`next` 仍指 3.0.0（账号级写操作，由所有者自行决定是否挪）；GitHub About 未改（产品描述未变）。
