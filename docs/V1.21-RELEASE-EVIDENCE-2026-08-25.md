# BuildBeat v1.21.0 发布关闭证据（2026-08-25）

> 证据等级：公开 GitHub / npm 分发事实 + 工作流外独立回读。本页证明 `@haiyangbg/buildbeat@1.21.0` 的精确源码、artifact、provenance、签名、安装和 Release 已闭合；不证明任何业务项目的 Gate、部署、生产健康或常态流量。

## 1. 源码、评审与不可移动 Tag

| 项目 | 回读结果 |
|---|---|
| 发布 PR | [#17](https://github.com/HaiYangBG1/BuildBeat/pull/17)，全部检查通过后正常合并 |
| main merge commit | `ce69a05c8a42b9ac8d3cafbc4ed224f0bbf71a63` |
| main CI | [run 32864194006](https://github.com/HaiYangBG1/BuildBeat/actions/runs/32864194006)，双平台脚本、Node 20/24、静态文档全部成功 |
| CodeQL | [run 32864196001](https://github.com/HaiYangBG1/BuildBeat/actions/runs/32864196001)，成功 |
| annotated tag | `v1.21.0`，tag object `78031f68a8c16f0ef7c3bb9daceda19431db34cb`，解引用后精确指向上述 merge commit |
| Tag 保护实时回读 | `Protect release tags` 为 active；include=`refs/tags/v*`；rules=`update,deletion`；bypass 为空 |

`v1.21.0` 创建前，官方 registry 对精确版本返回不存在。Tag 创建并推送后没有移动、删除或复用；本证据文档是发布后的 main 文档提交，不属于已发布 tarball，也不重新定义该 immutable artifact。

## 2. OIDC 发布工作流

[workflow run 32864438692](https://github.com/HaiYangBG1/BuildBeat/actions/runs/32864438692) 由 `main@ce69a05` 手动触发，输入为 `v1.21.0`：

- `Publish v1.21.0 to npm` job 成功：annotated tag、checkout HEAD、事件 SHA、远端 main HEAD、package version 与干净工作树全部一致；重新运行发布检查，打出一份精确候选，再通过 npm Trusted Publishing / OIDC 发布。
- `Verify v1.21.0 from npm` job 成功：从官方 registry 回读精确 integrity、SLSA provenance、隔离安装和 attestation 签名。
- `npm-publish` Environment 只允许受保护分支，并由配置的 reviewer 通过正式 pending-deployment 审批接口放行；没有使用管理员 bypass，也没有向仓库、命令或日志注入长期 npm publish token。

## 3. 官方 registry 与供应链回读

| 项目 | 回读结果 |
|---|---|
| package / version | `@haiyangbg/buildbeat@1.21.0` |
| 发布时间 | `2026-08-25T15:14:09.041Z` |
| dist-tags | `bootstrap=0.0.0`，`latest=1.21.0` |
| tarball 文件数 | `80`；工作流外本地 pack 与 registry integrity 完全一致 |
| integrity | `sha512-P9HluoPvb6/HJhh2IP3VLG3ig6kQ4aJKtkZeowcU6Virv9vGxgvulMnq9J7XN5dQNtcwaUZhfgLKR/Qi93e+wA==` |
| shasum | `6670c3c7e39f1901aa1db62c1579cf83b086dcc9` |
| attestation | `https://registry.npmjs.org/-/npm/v1/attestations/@haiyangbg%2fbuildbeat@1.21.0` |
| provenance predicate | `https://slsa.dev/provenance/v1` |
| 签名审计 | 1 个 verified registry signature；1 个 verified attestation |

registry README 也已从精确版本回读，包含 v1.21 的「证据紧跟对应的已做事项」回复契约；不是用本地 README 代替 registry 页面。

## 4. 工作流外隔离安装

从全新临时 prefix 安装精确版本后：

- `buildbeat --version` 返回 `1.21.0`；包内兼容入口 `solobaton --version` 也返回 `1.21.0`；
- `npm audit signatures` 返回上述 1 个 registry signature 和 1 个 attestation；
- 对没有 lifecycle manifest 的源码仓根运行已安装包的 `doctor . --json`，得到合法 schema 2 JSON：`ok=false`、`writesPerformed=false`，并诚实返回 exit `1`；
- 执行前后 Git 可见状态完全一致，没有把诊断失败伪装成绿灯，也没有写入项目。

## 5. GitHub Release 与边界

[BuildBeat v1.21.0](https://github.com/HaiYangBG1/BuildBeat/releases/tag/v1.21.0) 已发布，非 draft、非 prerelease，并成为 latest Release。Release 创建发生在工作流内验证和工作流外独立回读全部完成之后。

已验证结论只覆盖 `v1.21.0@ce69a05`、官方 npm artifact、供应链声明、隔离安装和 GitHub Release。npm dist-tag、Environment、ruleset 与 Release latest 标记仍是可变远端状态，后续发布必须重新实时回读；任何业务仓的升级、Gate、部署和生产状态仍需在其自身边界独立验证。
