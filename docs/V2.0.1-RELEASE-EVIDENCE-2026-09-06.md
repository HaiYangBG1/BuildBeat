# v2.0.1 发布证据（2026-09-06）

> 授权：所有者会话内「发」（发布 + `npm-publish` 环境审批 + GitHub Release 标 Latest + 本机 CLI 切到 `@latest`）；稳定版通道：从 `main` 顶端以稳定 tag 发布到 dist-tag `latest`。

- 内容：2.0.0 之后的对外说明同步四批——A 事实与合同（PR #24）、B 使用路径与 `templates/v2/`（PR #27）、D 防回退与维护文档（PR #26）、C 首页重写与接续指南（PR #28）——全部 squash 合入 `main`。运行时唯一行为修复：run 配置 `workers.<角色>.inheritEnv` / `env:` 经 CLI 加载路径透传到 Shell Adapter（2.0.0 在 `doctor` 报告姿态、`start` 却按默认白名单跑）。v1 生命周期命令与骨架 `v1.21` 不变；Claude 插件 manifest 0.2.1 → 0.2.2（描述变更）。
- 路径：release 提交在 `release/2.0.1`（`1c2dd26` 版本与文案 + `51c1ddc` 修 `tests/pack-firstrun.test.sh` 在 `npm publish --dry-run` 下继承 `npm_config_dry_run` 的问题）经 PR #29 以 merge commit `4b2362f` 合入 `main`，annotated tag `v2.0.1` 指向它；`v2` 分支 ff 到同一提交。本地候选门全过：`npm ci`、`bash -n`、`shellcheck -x`、`actionlint`、`check-docs`（180 文件）、`test-scripts` 228、`test:pilot` 15、`test:plugin` 7、`test:skill-only`、`test:pack-firstrun` 14、`npm test` 209/209、`npm publish --dry-run`（2.0.1，含 `prepublishOnly` 全链）、gitleaks 无泄漏、`git diff --check`；PR #29 与 `main` 顶端 CI/CodeQL 全部 success。发布前回读（直连 registry.npmjs.org）：`2.0.1` 不存在；dist-tags `{bootstrap: 0.0.0, latest: 2.0.0, next: 2.0.0-beta.5}`；tag ruleset `Protect release tags`：active、`refs/tags/v*`、`update,deletion`、bypass 为空。
- 发布：workflow run [34032278315](https://github.com/HaiYangBG1/BuildBeat/actions/runs/34032278315)（`workflow_dispatch` from `main`，tag `v2.0.1`，稳定通道 → `latest`）双 job success（`Publish v2.0.1 to npm` + `Verify v2.0.1 from npm`）；`npm-publish` 环境审批按会话授权以所有者 gh 凭据落章，备注引用授权原文。
- 本地独立回读（直连 registry.npmjs.org，不经镜像）：dist-tags `{bootstrap: 0.0.0, next: 2.0.0-beta.5, latest: 2.0.1}`；`dist.integrity = sha512-1/rUoBFtBljiOCuP5XEHLiGXQEOO4E4CJurQ2rsXP9KEJr0tCwBEAe3tp2P3MIH6ApoSM6CUEUcDI9mEkbDiDw==`（与发布前本地 `npm publish --dry-run` 打出的 integrity 逐字相同）；`dist.attestations.url` 存在；隔离前缀安装 `@2.0.1` → `buildbeat --version = 2.0.1`、`solobaton --version = 2.0.1`，`buildbeat-v2` usage 含 `--adopt`，`templates/v2/envelope/{worker.sh,prompts/builder|reviewer|fixer.md}` 与 `templates/v2/run-config.example.yaml` 在包内；`buildbeat doctor <空目录> --json` 返回有界 JSON（`schemaVersion 2`，`cliVersion 2.0.1`，exit 1，目录零写入）；临时项目 `npm audit signatures`：1 package has a verified attestation。
- GitHub：Release [v2.0.1](https://github.com/HaiYangBG1/BuildBeat/releases/tag/v2.0.1) 已建并标 **Latest**（此前 Latest 为 v2.0.0）。
- 切换：所有者本机全局 CLI `npm install --global @haiyangbg/buildbeat@latest`，`buildbeat --version = 2.0.1`，全局包内 `templates/v2/envelope/` 存在。
- 发布过程中发现并修正：`prepublishOnly` 里新加的打包首跑在 `npm publish --dry-run` 下失败（父 npm 把 `npm_config_dry_run` 传给嵌套 `npm pack`，不产 tarball）；真实发布走 pack 后 publish tarball 不经 `prepublishOnly`，不受影响；脚本清掉该变量后候选检查恢复（`51c1ddc`）。
- 未做：GitHub About 未改（对外动作，等所有者另行授权）；旧 `solobaton` 包不动；`next` 仍指 2.0.0-beta.5，留给后续预发布。
