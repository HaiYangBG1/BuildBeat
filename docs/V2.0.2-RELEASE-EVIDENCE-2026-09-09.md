# v2.0.2 发布证据（2026-09-09）

> 授权：所有者会话内「发 2.0.2」（发布 + `npm-publish` 环境审批 + GitHub Release 标 Latest + 本机 CLI 切到 `@latest`）；稳定版通道：从 `main` 顶端以稳定 tag 发布到 dist-tag `latest`。

- 内容：只有 PR #32——`package.json` 的 `files` 显式排除历史文档（发布证据、迭代记录、阶段试点、规划路线、v2 长文、`docs/v2/M1/M2/M4-*`），现行文档照常分发；包内 `docs/` 61 → 24 个文件，压缩约 497 kB → 363 kB，解压约 1.3 MB → 1.0 MB；`tests/pack-firstrun.test.sh` 新增两条断言守两边。运行时、v1 生命周期命令、骨架 `v1.21`、Claude 插件 `0.2.2` 均不变。
- 路径：release 提交 `24a5213` 在 `release/2.0.2`（版本号、CHANGELOG 标题、`docs/CLI.md` / `docs/CAPABILITY-MATRIX.md`（此前仍写 2.0.0）/ `docs/RELEASING.md` 状态行、example manifest `cliVersion`）经 PR #33 以 merge commit `a077367` 合入 `main`，annotated tag `v2.0.2` 指向它。本地候选门全过：`npm ci`、`bash -n`、`shellcheck -x`、`actionlint`、`check-docs`（181 文件）、`test-scripts`、`test:plugin`、`test:pilot`、`test:skill-only`、`npm test` 209/209、gitleaks 无泄漏、`git diff --check`、`npm publish --dry-run`（2.0.2，含 `prepublishOnly` 全链，`test:pack-firstrun` 16 条）；PR #33 8 项 check 与 `main` 顶端 CI/CodeQL 全部 success。发布前回读（直连 registry.npmjs.org）：`2.0.2` 不存在；dist-tags `{bootstrap: 0.0.0, next: 2.0.0-beta.5, latest: 2.0.1}`；tag ruleset `Protect release tags`：active、`refs/tags/v*`、`update,deletion`、bypass 为空。
- 发布：workflow run [34351668694](https://github.com/HaiYangBG1/BuildBeat/actions/runs/34351668694)（`workflow_dispatch` from `main`，tag `v2.0.2`，稳定通道 → `latest`）双 job success（`Publish v2.0.2 to npm` + `Verify v2.0.2 from npm`）；`npm-publish` 环境审批按会话授权以所有者 gh 凭据落章，备注引用授权原文与候选 integrity。
- 本地独立回读（直连 registry.npmjs.org，不经镜像）：dist-tags `{bootstrap: 0.0.0, next: 2.0.0-beta.5, latest: 2.0.2}`；`dist.integrity = sha512-OAuzt8Gb+6y+xOjGVn/q+hifaOWqcMUrWKyGNM5U2oGfRFEsUxp/m7qXSHt4NgDBTC7jjN/GnOafVJjeBI4+fA==`（与发布前本地 `npm pack --dry-run --json` 打出的 integrity 逐字相同，shasum `7994962d`），`fileCount 137`、`unpackedSize 1016340`；`dist.attestations.url` 存在；隔离前缀安装 `@2.0.2` → `buildbeat --version = 2.0.2`、`solobaton --version = 2.0.2`，`buildbeat-v2` 打印 usage；包内 `docs/` 24 个文件，历史文档 0 个，`docs/README.md`、`docs/v2/guide/01-quickstart.md`、`templates/v2/envelope/worker.sh`、`templates/v2/run-config.example.yaml` 在包内；`buildbeat doctor <空目录> --json` 返回有界 JSON（`schemaVersion 2`，`cliVersion 2.0.2`，exit 1，目录零写入）；临时项目 `npm audit signatures`：1 package has a verified registry signature / verified attestation。
- GitHub：Release [v2.0.2](https://github.com/HaiYangBG1/BuildBeat/releases/tag/v2.0.2) 已建并标 **Latest**（此前 Latest 为 v2.0.1；`releases/latest` 回读 = v2.0.2）。
- 切换：所有者本机全局 CLI `npm install --global @haiyangbg/buildbeat@latest` → 2.0.2；`v2` 分支 ff 到 `main`。
- 未做：GitHub About 不需改（产品描述未变）；旧 `solobaton` 包不动；`next` 仍指 2.0.0-beta.5，留给后续预发布。
