# v3.0.0 发布证据（2026-09-09）

> 授权：所有者会话内「合并，然后发 3.0.0」（合并 PR #36、发布 + `npm-publish` 环境审批 + GitHub Release 标 Latest + 本机 CLI 切到 `@latest`）；稳定版通道：从 `main` 顶端以稳定 tag 发布到 dist-tag `latest`。

- 内容：PR #36（merge commit `f6f33ee`）——破坏性变更，仓库与包只剩一个产品：可执行文件只有 `buildbeat`（原 `buildbeat-v2`，新增 `--version`），`buildbeat-v2` 与 `solobaton` 删除；v1 生命周期 CLI 源码、文件总线模板与脚本、`example/`、`docs/CLI.md` / `CHECKS.md` / `LEGACY-V1.16-MIGRATION.md`、`guide/08-migration-v1.md`、`legacy-four-gates` 预设及其测试全部移除；SKILL、lessons、能力矩阵、指南、模板改为单一产品口径；Claude 插件 0.2.2 → 0.3.0。需要 v1 的项目停在 2.0.2。
- 路径：release 提交 `d446b73` 在 `release/3.0.0`（版本号、CHANGELOG 标题、`docs/RELEASING.md` 源版本行）经 PR #37 以 merge commit `0289415` 合入 `main`，annotated tag `v3.0.0` 指向它。本地候选门全过：`npm ci`、`bash -n`、`shellcheck -x`、`actionlint`、`check-docs`（107 文件）、`npm test` 159/159、`test:pilot` 15、`test:plugin` 7、gitleaks 无泄漏、`git diff --check`、`npm publish --dry-run`（含 `prepublishOnly` 全链，`test:pack-firstrun` 17 条）；PR #36、#37 与 `main` 顶端 CI/CodeQL 全部 success。发布前回读（直连 registry.npmjs.org）：`3.0.0` 不存在（E404）；dist-tags `{bootstrap: 0.0.0, next: 2.0.0-beta.5, latest: 2.0.2}`；tag ruleset `Protect release tags`：active、`refs/tags/v*`、`update,deletion`、bypass 为空。
- 发布：workflow run [34362068004](https://github.com/HaiYangBG1/BuildBeat/actions/runs/34362068004)（`workflow_dispatch` from `main`，tag `v3.0.0`，稳定通道 → `latest`）。`Publish v3.0.0 to npm` 一次 success（`npm publish` 回 `+ @haiyangbg/buildbeat@3.0.0`，provenance 进 sigstore 透明日志 logIndex 2769951682）；`npm-publish` 环境审批按会话授权以所有者 gh 凭据落章，备注引用授权原文与候选 integrity。**`Verify v3.0.0 from npm` 首次 failure**：npm 在 publish 输出里提示 "Your package is being processed and may take a few minutes to become available"，registry 约 6 分钟后（22:19 本地时间）才出现 3.0.0，而验证作业的精确回读只等 24×5 秒。版本可见后 `gh run rerun --failed` 重跑验证作业 → success，run 结论 success；没有二次发布。
- 本地独立回读（直连 registry.npmjs.org，不经镜像）：dist-tags `{bootstrap: 0.0.0, next: 2.0.0-beta.5, latest: 3.0.0}`；`dist.integrity = sha512-C2ltksdWn4aJiQdi8QMWHkU3lGypi5VfqDlFhumg9MqpfUsYH4KlgloEWGgwZt560NC8Qv2PDO/qcno6lwGAFA==`（与发布前本地 `npm pack --dry-run --json` 逐字相同，shasum `1d5e74c1`），`fileCount 85`、`unpackedSize 578019`；`dist.attestations.url` 存在；隔离前缀安装 `@3.0.0` → `bin/` 下只有 `buildbeat`，`buildbeat --version = 3.0.0`，裸调用打印 `BuildBeat runtime` 用法、exit 0、对空目录零写入；包内 `src/` 只有 `v2/`，无 `example/`、无 `templates/scripts/`，`docs/` 20 个文件，`templates/v2/envelope/worker.sh`、`run-config.example.yaml`、`templates/pm/decisions.md`、快速开始在包内；临时项目 `npm audit signatures`：1 package has a verified registry signature / verified attestation。
- GitHub：Release [v3.0.0](https://github.com/HaiYangBG1/BuildBeat/releases/tag/v3.0.0) 已建并标 **Latest**（`releases/latest` 回读 = v3.0.0；此前 Latest 为 v2.0.2）。
- 切换：所有者本机全局 CLI `npm install --global @haiyangbg/buildbeat@latest` → `buildbeat --version = 3.0.0`，PATH 上不再有 `buildbeat-v2` / `solobaton`；`v2` 分支 ff 到 `main`。
- 发布过程中发现并修正：验证作业的三个回读循环（精确版本、dist-tag、provenance）等待窗口按 npm 异步处理拉长到 10 分钟（本次 closeout 随附）；发布手册补一句"只在回读上失败时，版本可见后重跑失败作业，不重新发布"。
- 未做：GitHub About 未改（产品描述未变；topics 里若有旧名由所有者另行决定）；`next` 仍指 2.0.0-beta.5，留给后续预发布。
